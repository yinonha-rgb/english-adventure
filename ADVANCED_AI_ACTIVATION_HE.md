# הפעלה עתידית של מורה ה־AI המתקדם

## המצב כעת

הגרסה המפורסמת היא גרסת `mock-only`. מורה ה־AI האמיתי נעול בכמה שכבות, אין endpoint פעיל, אין סוד בדפדפן, לא נפרס backend בתשלום ולא מתבצעת קריאת OpenAI. מצב ההדגמה מסומן בבירור: **„מצב הדגמה – ללא שימוש ב־API”**.

## תנאים שחייבים להתקיים לפני הפעלה

1. לקבל מבעל הפרויקט אישור מפורש להפעלה ולבדיקת API מוגבלת.
2. לבדוק באותו יום את המחירים הרשמיים העדכניים של OpenAI. אין להעתיק מחירים ישנים מהתיעוד או מהקוד.
3. להגדיר מגבלות נמוכות: דקות ליום ולחודש, מפגש אחד ביום, עלות מרבית למפגש ותקציב חודשי.
4. להפעיל Firebase billing ו־Functions ידנית רק לאחר האישור.
5. לשמור את מפתח OpenAI ב־Firebase Secret Manager בלבד. אין להכניס אותו ל־GitHub, ל־JavaScript של הדפדפן, ל־Firestore או ללוגים.
6. לפרוס את `teacherApi`, לאמת Firebase Authentication, הרשאת הילד, הסכמת הורה, App Check, מגבלות ותקציב בצד השרת.
7. ליצור אסימון הפעלה אקראי בצד השרת, להגדיר `ADVANCED_AI_ENABLED=true`, `BILLING_APPROVED=true`, endpoint ו־build mode של production רק בגרסת הפעלה ייעודית.
8. לבצע בדיקה אחת קצרה ומבוקרת, לוודא רישום שימוש והתראות 50%/75%/90%, ואז לעצור ולבדוק את החיוב בפועל.

## חזרה בטוחה

בכל כשל, חריגה, ניתוק או סיום תקציב יש לחזור אוטומטית למורה החינמי. אין למחוק נתוני לימוד, אין לשמור אודיו גולמי כברירת מחדל, ואין לאפשר להגדרות מקומיות או ל־Firestore לפתוח את נעילת ה־build.

## נקודת האישור

אין לבצע את השלבים בתשלום עד שהבעלים כותב בדיוק:

> אני מאשר להפעיל את מורה ה־AI האמיתי ולבצע בדיקת API מוגבלת.
