# הגדרת מורה AI מתקדם

המורה המתקדם כבוי כברירת מחדל. אין להפעיל אותו לפני השלמת כל השלבים ואישור מפורש של בעל הפרויקט.

## לפני שמתחילים

השימוש במורה AI מתקדם עשוי להיות כרוך בתשלום לפי שימוש. מנוי ChatGPT Plus אינו כולל שימוש ב־OpenAI API. יש להגדיר תקציב והתראות בחשבון OpenAI וב־Google Cloud Billing.

## שלבי הפעלה

1. שדרגו את Firebase לתוכנית Blaze רק לאחר אישור חיוב מפורש.
2. צרו מפתח OpenAI API. לעולם אין להדביק אותו ב־GitHub, בקוד הדפדפן, ב־Firestore או ב־`firebase-config.js`.
3. שמרו אותו כסוד Functions בשם `OPENAI_API_KEY`:
   `firebase functions:secrets:set OPENAI_API_KEY`
4. פרסו את הפונקציה המאובטחת רק לאחר אישור:
   `firebase deploy --only functions,firestore:rules`
5. העתיקו את כתובת `teacherApi` אל `teacherAIConfig.endpoint` והגדירו `demoMode: false`.
6. באזור ההורים הפעילו `מורה AI מתקדם` וסמנו הסכמה מפורשת.

השרת מאמת Firebase UID, בודק הסכמת הורה ומכסות, ומחזיר אישור קצר־חיים בלבד. אין שמירת שמע גולמי.

## חזרה למצב חינמי

באזור ההורים בטלו את `מורה AI מתקדם` ובחרו `מורה חינמי` כברירת המחדל. אפשר גם להשאיר את endpoint ריק או להחזיר `demoMode: true`. השיעורים הרגילים והמורה החינמי ימשיכו לעבוד.
