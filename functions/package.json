{
  "name": "english-adventure-functions",
  "private": true,
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "deploy": "firebase deploy --only functions,firestore:rules"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
